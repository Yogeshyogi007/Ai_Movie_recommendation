services:
  - type: web
    env: python
    buildCommand: |
      apt-get update && apt-get install -y build-essential gfortran libatlas-base-dev
      pip install --upgrade pip
      pip install -r requirements.txt
